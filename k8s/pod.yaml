apiVersion: v1
kind: Pod
metadata:
  name: nginx-2
  labels:
    app: nginx-2
    tier: frontend
spec:
  containers:
  - name: nginx-2
    image: nginx:latest
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80